# MVP1-FEAT-001 ログイン機能 実装ログ

## 機能概要

* 機能ID: `MVP1-FEAT-001`
* 機能名: ログイン機能
* 対象UI: Linknest user-ui
* 参照資料:
  * `README.md`
  * `login-flow.png`

---

## 実装履歴

---

### [工程1] /login 静的UI実装

- **実施日**: 2026-05-29
- **担当**:
  - 初期静的UI実装: Claude Code
  - WF確認・実機表示確認・CSS調整: ユーザー

#### 実装内容

- `/login` 画面の初期表示状態を静的UIとして実装した
- WF「1. ログイン画面」の全表示要素を実装対象とした

#### 変更ファイル

| ファイル | 種別 |
|---|---|
| `src/app/login/page.tsx` | 新規作成 |
| `src/app/login/page.module.css` | 新規作成 |

#### 設計判断

- カードUI（白背景・角丸・シャドウ）はWFに存在しないため追加しなかった
- レイアウトはすべて CSS Grid で実装。`display: flex` は使用していない
- パスワード入力欄の目アイコンは `grid-area: 1 / 1 / 2 / 2` による同一グリッドセル重ねで配置し、`position: absolute` は使用していない
- クラス名はケバブケースBEM（`.login-page`, `.login-page__*`）に統一
- ログインボタンは静的UIのため `type="button"`
- PC表示時にフォーム横幅が過度に広がらないよう、`.login-page__content` に `max-width: 460px` と `justify-self: center` を設定して中央配置とした
- `href="#"` はプレースホルダー。実際のリンク先は今後の工程で設定する

#### 未実装事項（次工程以降）

- 入力状態管理
- UIバリデーション
- 入力欄・ボタン・リンクのフォーカス表示（`:focus-visible`）対応
- 認証失敗表示
- アカウントロックモーダル
- API連携
- 画面遷移

#### 確認結果

- `npm run lint`: エラーなし
- `npm run build`: 成功（`/login` Static生成）
- ブラウザでの初期表示確認済み
- iPhone 16 Pro Max 実機からLAN経由での表示確認済み（ユーザーによるCSS調整完了）

#### 次の作業

- 工程2: 入力状態管理（`useState` によるメールアドレス・パスワード制御）

---

### [工程2] 入力状態管理・パスワード表示切替・フォーカス表示対応

- **実施日**: 2026-05-30
- **担当**:
  - 初期実装: Claude Code
  - 動作確認・フォーカス表示調整: ユーザー

#### 実装内容

- メールアドレス入力値の状態管理
- パスワード入力値の状態管理
- パスワード表示／非表示切替
- 表示状態に応じた目アイコンおよび `aria-label` の切替
- 入力欄・ボタン・リンクのフォーカス表示対応

#### 変更ファイル

| ファイル | 種別 |
|---|---|
| `src/app/login/page.tsx` | 変更 |
| `src/app/login/page.module.css` | 変更 |
| `next.config.ts` | 変更（iPhone実機確認用の開発設定として `allowedDevOrigins` を追加） |

#### 設計判断

- `useState` を利用するため `page.tsx` を Client Component 化した
- 現段階ではコンポーネント分割を行わず、既存ページ内で状態管理を追加した
- ログインボタンは送信処理未実装のため `type="button"` を維持した
- Inputはタップ・クリック時にも入力中であることが分かるよう、`:focus` でフォーカス表示を行う
- ボタンおよびリンクは、主にキーボード操作時の視認性確保を目的として `:focus-visible` でフォーカス表示を行う
- iPhone実機からLAN経由でNext.js開発画面を確認するため、`allowedDevOrigins` にPCのLAN内IPを追加した

#### 未実装事項（次工程以降）

- UIバリデーション
- ログインボタンの活性・非活性制御
- API連携
- 認証失敗表示
- アカウントロック表示
- 画面遷移

#### 確認結果

- `npm run lint`: エラーなし
- `npm run build`: 成功
- PCブラウザでメールアドレス・パスワード入力確認済み
- PCブラウザでパスワード表示／非表示切替確認済み
- iPhone 16 Pro Max実機で表示およびパスワード表示／非表示切替確認済み

#### 次の作業

- 工程3: ログインボタン活性・非活性制御

---

### [工程3] ログインボタン活性・非活性制御

- **実施日**: 2026-06-01
- **担当**:
  - 実装: Claude Code
  - 動作確認: ユーザー

#### 実装内容

- メールアドレスとパスワードの入力状態に応じたログインボタンの活性・非活性制御
- メールアドレスが1文字以上、かつパスワードが1文字以上入力されている場合のみログインボタンを活性化
- 空白のみの入力では活性化しないよう `trim()` を使用
- 非活性状態では `disabled` 属性を付与
- 非活性状態のログインボタンスタイルを追加

#### 変更ファイル

| ファイル | 種別 |
|---|---|
| `src/app/login/page.tsx` | 変更 |
| `src/app/login/page.module.css` | 変更 |

#### 設計判断

- 新規stateは追加せず、既存の `email` / `password` から `isLoginEnabled` を算出した
- 動作制御は `disabled` 属性、見た目制御は `.--disabled` Modifierで行った
- 非活性スタイルには `--color-button-disabled` と `--color-button-disabled-text` を使用した
- メールアドレス形式チェック、パスワード文字数チェック、blur時バリデーションは今回実装しない方針とした

#### 未実装事項（次工程以降）

- メールアドレス形式チェック
- パスワード文字数チェック
- blur時のバリデーション
- 入力エラーメッセージ表示
- API連携
- 認証失敗表示
- アカウントロック表示
- ログイン成功後の画面遷移

#### 確認結果

- `npm run lint`: エラーなし
- `npm run build`: 成功
- 初期表示時にログインボタンが非活性であることを確認
- メールアドレスのみ入力時は非活性であることを確認
- パスワードのみ入力時は非活性であることを確認
- メールアドレスとパスワードの両方を入力した場合のみ活性化することを確認
- 空白のみの入力では活性化しないことを確認
- PCおよびiPhone実機で表示・動作確認済み